# AGENTS.md instructions

## Never Do These

- Never run dev servers. The user can run them.
- Never use `any` types when using TypeScript.

## Always Do These

- Always use `bun` when possible.
- Always run the relevant CI checks before saying the work is done.
- For pull requests, mirror the repo's GitHub Actions workflow locally as closely as practical before reporting completion.
